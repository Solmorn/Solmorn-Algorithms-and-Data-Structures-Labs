#ifndef POINT1_SORTS_H
#define POINT1_SORTS_H

#include <stddef.h>

void insertion_sort(int* arr, size_t n);
void bubble_sort(int* arr, size_t n);
void selection_sort(int* arr, size_t n);
void shell_sort(int* arr, size_t n);

#endif